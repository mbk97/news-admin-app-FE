export interface ISidebarTypes {
  id: number;
  title: string;
  Icon: any;
  link: string;
}

export interface IRoles {
  _id: string;
  roleName: string;
  createdAt: string;
  updatedAt: string;
  __v: 0;
}

export interface IUser {
  _id: string;
}
